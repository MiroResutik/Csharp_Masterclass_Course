namespace Domain
{
    public class Calculator
    {
        public int Sum(int x, int y)
        {
            //return 5; // will throw error
            return x + y; // will pass the test
        }
    }
}
